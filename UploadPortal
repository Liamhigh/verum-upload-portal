
import React from 'react';

function UploadPortal({ context }) {
  return (
    <div style={{ border: "1px solid #ccc", padding: "1rem" }}>
      <h2>Upload Portal</h2>
      <p>Active Context: <strong>{context}</strong></p>
      <input type="file" />
    </div>
  );
}

export default UploadPortal;
